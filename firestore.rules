rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype/demo rules: any authenticated user can read/write app data.
    // This restores the pre-role-change behavior.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
